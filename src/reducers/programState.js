/**
 * Reducer for keeping tabs on the program state
 */
const programState = (state = "loading", action) => {
    switch (action.type){
        default:
            return state;
    }
}

export default programState;